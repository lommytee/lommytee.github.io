---
title: Tommy Lee
---

<h1>TOMMY LEE</h1>

</hr>

<ul>
  <% data.movies.items.each_with_index do |item, index| %>
  <li>
    <% if item.link.empty? %>
      <%= item.title %>
      |
      <%= item.role %>
    <% else %>
      <%= link_to(
          item.title,
          item.link) %>
      |
      <%= item.role %>
    <% end %>
    <% if index == 0 or index == 3 %>
      </li>
      </ul>
      <ul>
    <% end %>
  </li>
  <% end %>
</ul>

<hr>
<p>Full site coming soon.</p>
